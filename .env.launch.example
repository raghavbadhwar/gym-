# Launch gate runtime template (example placeholders only; never commit real values)
# Strict launch gate REQUIRED:
JWT_SECRET=replace-with-64-char-random-hex
JWT_REFRESH_SECRET=replace-with-64-char-random-hex
REDIS_URL=redis://user:pass@host:6379/0
# At least one DSN must be set for strict gate
SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
GATEWAY_SENTRY_DSN=

# Launch hardening REQUIRED for production runtime:
NODE_ENV=production
ALLOW_DEMO_ROUTES=false
REQUIRE_DATABASE=true
REQUIRE_QUEUE=true
ALLOWED_ORIGINS=https://gateway.example.com,https://issuer.example.com,https://wallet.example.com,https://recruiter.example.com
DATABASE_URL=postgresql://user:pass@host:5432/credity

# Issuer signing/encryption + chain wiring:
ISSUER_KEY_ENCRYPTION=replace-with-64-char-hex
# Optional while rotating encryption key material
ISSUER_KEY_ENCRYPTION_PREVIOUS=replace-with-old-64-char-hex[,older-key]
RELAYER_PRIVATE_KEY=0xreplace-with-32-byte-hex
CHAIN_NETWORK=ethereum-sepolia
SEPOLIA_RPC_URL=https://your-sepolia-rpc
REGISTRY_CONTRACT_ADDRESS=0xreplace-with-sepolia-or-prod-registry-address

# Reputation graph (Neo4j)
REPUTATION_GRAPH_ENABLED=false
REPUTATION_GRAPH_URI=bolt://neo4j-host:7687
REPUTATION_GRAPH_USERNAME=neo4j
REPUTATION_GRAPH_PASSWORD=replace-with-neo4j-password
REPUTATION_GRAPH_DATABASE=neo4j

# Wallet trust-sdk bridge (BlockWalletDigi -> Issuer reputation APIs)
REPUTATION_TRUST_SDK_ENABLED=false
TRUST_SDK_BASE_URL=https://issuer.example.com/api
TRUST_SDK_API_KEY=
TRUST_SDK_TIMEOUT_MS=10000

# Gateway OAuth (required when Google sign-in is enabled):
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://gateway.example.com/api/auth/google/callback
